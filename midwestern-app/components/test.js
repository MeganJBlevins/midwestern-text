export default function Test() {
  return (
    <section className="test">
      <div className="container">
        <h1>Heading One</h1>
        <p>Remove the duplicates in 2 javascript objects and output the list of distinct names in an aunordered list when this <a>link is clicked</a>. If the operation has been completed already notify the user that this has already been done.</p>
        
      </div>
    </section>
  )
}
